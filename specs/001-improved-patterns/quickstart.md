# 快速入门指南: 改进的 Python 设计模式项目

**版本**: 1.0.0
**日期**: 2025-11-20

## 概述

欢迎使用改进的 Python 设计模式学习项目!本指南将帮助你在 5 分钟内开始学习设计模式。

## 前置要求

### 必需
- Python 3.8 或更高版本
- pip (Python 包管理器)
- Git (用于克隆项目)

### 可选
- 虚拟环境工具 (venv, virtualenv, 或 conda)

### 验证环境

```bash
# 检查 Python 版本
python --version  # 应显示 3.8 或更高

# 检查 pip
pip --version

# 检查 Git
git --version
```

## 安装步骤

### 步骤 1: 克隆项目

```bash
# 克隆仓库
git clone https://github.com/your-username/improved-patterns.git

# 进入项目目录
cd improved-patterns
```

### 步骤 2: 创建虚拟环境 (推荐)

```bash
# 使用 venv 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Linux/macOS:
source venv/bin/activate

# Windows:
venv\Scripts\activate
```

### 步骤 3: 安装依赖

```bash
# 安装项目及其依赖
pip install -e .

# 或者仅安装开发依赖
pip install -e ".[dev]"
```

### 步骤 4: 验证安装

```bash
# 检查 CLI 是否可用
patterns --version

# 应该显示:
# patterns 1.0.0
```

## 快速开始: 5 分钟教程

### 1. 列出所有模式 (30 秒)

```bash
# 查看所有可用的设计模式
patterns list

# 输出示例:
# 创建型模式 (Creational Patterns)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ID                  中文名称    描述
# ──────────────────────────────────────────
# singleton           单例模式    确保一个类只有一个实例
# factory             工厂模式    定义创建对象的接口
# ...
```

### 2. 运行你的第一个示例 (1 分钟)

```bash
# 运行单例模式示例
patterns run singleton

# 输出示例:
# === 运行模式: 单例模式 (Singleton) ===
#
# 创建第一个实例...
# 创建第二个实例...
# 两个实例是同一个对象: True
#
# ✓ 示例执行成功 (耗时: 0.023s)
```

### 3. 查看文档 (2 分钟)

```bash
# 查看单例模式的完整文档
patterns docs singleton

# 输出示例:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 单例模式 (Singleton Pattern)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 【意图】
# 确保一个类只有一个实例,并提供全局访问点。
#
# 【适用场景】
# 1. 需要严格控制全局状态访问时
# 2. 系统中某个类只应该有一个实例
# ...
```

### 4. 对比相似模式 (1 分钟)

```bash
# 对比单例模式和 Borg 模式
patterns compare singleton borg

# 输出示例:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 模式对比: 单例模式 vs Borg 模式
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 【主要区别】
# • 单例模式确保只有一个实例
# • Borg 模式允许多个实例但共享状态
# ...
```

### 5. 运行测试 (30 秒)

```bash
# 运行单例模式的测试
patterns test singleton

# 输出示例:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 运行测试: 单例模式
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# test_singleton.py::test_single_instance ✓
# test_singleton.py::test_global_access ✓
#
# 2 个测试通过 (耗时: 0.152s)
```

## 常见使用场景

### 场景 1: 学习特定模式

```bash
# 1. 列出特定分类的模式
patterns list creational

# 2. 运行感兴趣的模式示例
patterns run factory

# 3. 查看详细文档
patterns docs factory

# 4. 查看源代码
cat patterns/creational/factory.py
```

### 场景 2: 按需求搜索模式

```bash
# 搜索解决"对象创建"问题的模式
patterns search 对象创建

# 搜索适用于"缓存"场景的模式
patterns search 缓存 --in use_cases
```

### 场景 3: 对比学习

```bash
# 对比工厂模式和抽象工厂模式
patterns compare factory abstract_factory

# 对比观察者模式、发布-订阅模式和中介者模式
patterns compare observer pubsub mediator
```

### 场景 4: 验证理解

```bash
# 运行完整测试套件
patterns test

# 运行特定模式的测试并查看覆盖率
patterns test observer --coverage
```

## 项目结构

```
improved-patterns/
├── patterns/            # 设计模式示例代码
│   ├── creational/      # 创建型模式
│   ├── structural/      # 结构型模式
│   ├── behavioral/      # 行为型模式
│   └── other/           # 其他模式
│
├── docs/                # 中文文档
│   ├── creational/
│   ├── structural/
│   ├── behavioral/
│   └── index.md         # 模式索引
│
├── cli/                 # 命令行工具
├── tests/               # 测试套件
├── config/              # 配置文件
└── README.md            # 项目说明
```

## 学习路径建议

### 初学者路径

1. **Week 1: 创建型模式**
   ```bash
   patterns list creational
   # 依次学习: singleton → factory → builder
   ```

2. **Week 2: 结构型模式**
   ```bash
   patterns list structural
   # 依次学习: adapter → decorator → proxy
   ```

3. **Week 3: 行为型模式**
   ```bash
   patterns list behavioral
   # 依次学习: strategy → observer → command
   ```

### 进阶路径

1. **按问题域学习**
   ```bash
   # 对象创建相关
   patterns search 对象创建

   # 对象组合相关
   patterns search 组合

   # 算法封装相关
   patterns search 算法
   ```

2. **对比学习**
   ```bash
   # 学习相似模式的区别
   patterns compare factory abstract_factory
   patterns compare decorator proxy
   patterns compare strategy state
   ```

## 常见问题

### Q1: 如何更新项目?

```bash
cd improved-patterns
git pull origin main
pip install -e . --upgrade
```

### Q2: 如何贡献代码或报告问题?

请访问 GitHub 仓库:
- 提交 Issue: https://github.com/your-username/improved-patterns/issues
- 提交 Pull Request: https://github.com/your-username/improved-patterns/pulls

### Q3: 如何离线使用?

所有文档和示例都在本地,无需网络连接。只需克隆项目后即可完全离线使用。

### Q4: 支持哪些 Python 版本?

支持 Python 3.8 及以上所有版本。使用以下命令检查兼容性:

```bash
python --version
```

### Q5: 如何自定义配置?

创建配置文件 `~/.patterns/config.yaml`:

```yaml
default_format: table
color: true
test_parallel: 4
```

## 性能提示

### 加速测试运行

```bash
# 使用并行测试
patterns test --parallel 4

# 使用 failfast 模式
patterns test --failfast
```

### 优化 CLI 响应

```bash
# 使用简单格式输出
patterns list --format simple

# 限制搜索范围
patterns search 关键词 --in name
```

## 下一步

恭喜!你已经掌握了基本使用方法。现在你可以:

1. 📚 **深入学习**: 选择一个模式,运行示例并阅读文档
2. 💻 **实践应用**: 在自己的项目中应用学到的模式
3. 🧪 **运行测试**: 通过测试代码加深理解
4. 🔍 **探索源码**: 阅读 `patterns/` 目录下的源代码
5. 📖 **阅读文档**: 查看 `docs/` 目录下的完整文档

## 获取帮助

- **CLI 帮助**: `patterns --help` 或 `patterns <命令> --help`
- **文档索引**: `patterns docs index` 或查看 `docs/index.md`
- **问题报告**: GitHub Issues
- **社区讨论**: GitHub Discussions

## 反馈

我们欢迎任何反馈和建议!请通过以下方式联系我们:

- GitHub Issues: 报告 bug 或请求新功能
- Pull Requests: 贡献代码或文档
- Discussions: 提问或分享经验

---

**祝学习愉快! 🎉**

如有任何问题,请随时查阅文档或寻求帮助。
